# Cloudflare Pages Redirects Configuration
# This file handles client-side routing for React Router

# Legacy GoHighLevel page URLs (indexed by Google before the domain cutover).
# 301 them to the homepage so old links never dead-end on the 404 page.
/multi-purpose-8872-8481    /    301

# Redirect all routes to index.html for single-page application
# The 200 status code serves the file without changing the URL
/*    /index.html   200
